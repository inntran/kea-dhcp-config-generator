fingerprint_library_version: "0.1.0"

dhcp4:
  valid-lifetime: 3600
  dns-servers: ["8.8.8.8"]

  control-sockets:
    - socket-type: http
      socket-address: 127.0.0.1
      socket-port: 8004

  subnets:
    - subnet: 10.0.0.0/24
      pools:
        - range: auto
          skip-start: 10
